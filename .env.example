# Staging gate (optional). Set VITE_STAGING_GATE=true in GitHub Actions vars for preview builds.
# VITE_STAGING_GATE=false

# SHA-256 hex digest of staging password (GitHub Actions secret). Generate locally:
# node -e "const c=require('crypto');console.log(c.createHash('sha256').update('YOUR_PASSWORD').digest('hex'))"
# VITE_STAGING_PASSWORD_HASH=
